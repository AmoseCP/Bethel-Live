# Bethel Live 使用手册

Bethel Live 是为教会直播设计的 YouTube 推流软件：一键创建直播、预览画面、检测声音、定时开播，并把直播链接一键分享到 Telegram 群。

---

## 1. 安装

- **Windows**：运行 `Bethel Live Setup x.x.x.exe` 安装包，按向导完成安装。
- **macOS**：打开 `.dmg`，把 Bethel Live 拖入「应用程序」文件夹。首次打开如提示未验证开发者，请在「系统设置 → 隐私与安全性」中点「仍要打开」。

> 软件依赖 FFmpeg 进行推流。若安装包未内置，请先安装：macOS 执行 `brew install ffmpeg`；Windows 从 gyan.dev 下载并加入 PATH。

## 2. 首次配置

### 2.1 Google API 凭据（必需）
1. 打开 [Google Cloud Console](https://console.cloud.google.com/)，创建项目。
2. 「API 和服务 → 库」中启用 **YouTube Data API v3**。
3. **配置 OAuth 同意屏幕**（「API 和服务 → OAuth 同意屏幕」）：用户类型选 **External（外部）**，填写应用名称与联系邮箱后保存。
4. **添加测试用户**：在同意屏幕的「测试用户（Test users）」中添加**教会的 Google 账号邮箱**——未通过 Google 审核的应用只有测试用户能授权，漏掉这一步授权页会提示「访问被阻止：应用未完成验证」。
5. 「API 和服务 → 凭据」中创建 **OAuth 客户端 ID**（应用类型选「桌面应用」）。
6. 把生成的 **Client ID** 与 **Client Secret** 填入 Bethel Live 的「设置 → Google API 凭据」，点「保存设置」。
7. 回到「直播」页，点「连接 YouTube 账号」，在弹出的浏览器中用**已加为测试用户**的教会账号授权（只需一次）。

> 注意：YouTube 账号需已开通直播权限（首次开通需在 YouTube 中申请并等待 24 小时）。

### 2.2 Telegram 分享（可选）
1. 在 Telegram 中找 **@BotFather** 创建机器人，获得 **Bot Token**。
2. 把机器人拉进目标群组，并给它发言权限。
3. 获取群组 **Chat ID**（可向 @userinfobot 等工具查询，群组 ID 通常以 `-100` 开头）。
4. 将两者填入「设置 → Telegram」，保存。

### 2.3 采集设备与屏幕
- 「设置 → 采集设备」中，从**检测到的设备列表**里选择默认视频设备（如 StreamLine Mini+ GC311G2 采集卡）和默认音频设备（如 Focusrite USB 声卡），点「保存设置」后**一直生效**，除非你再次修改。
- 未做过选择时，软件使用列表中的第一个设备；已保存的设备被拔出时，也会临时回落到第一个可用设备。
- 「屏幕直播采集的屏幕」同样从检测到的屏幕列表中选择（如外接投影屏），保存后一直生效；未选择时默认主屏幕。
- macOS 首次使用会弹出摄像头/麦克风授权，请点允许；屏幕直播还需在「系统设置 → 隐私与安全性 → 屏幕录制」中勾选 Bethel Live。

## 3. 开始直播

1. **预览**：打开软件即可在「直播」页看到画面预览与音频电平表。说话时电平条应随声音跳动；如提示「持续无声」，请检查声卡连接与系统输入音量。点电平表旁的「🎧 试听」可在本机实际听到采集的声音（**请佩戴耳机**，外放会啸叫），确认音质后再开播。
2. **选视频源**：右上角在「📷 摄像机」与「🖥 本机屏幕」之间切换。**直播进行中也可以随时切换**——切换时观众画面会短暂停顿 1~3 秒后自动恢复，直播不会中断。
3. **标题**：按当天日期自动生成（如周日为 `8/2/2026 Sunday Service`，周三/周五提供早晚两场可选），可手动改成任意标题。
4. 点 **🚀 一键创建直播** → 生成直播与分享链接。直播默认为**不公开列出（unlisted）**——只有拿到链接的人能观看，不会出现在频道页和搜索里；并默认标记为**非儿童内容（not made for kids）**。如需公开直播，可事后在 YouTube Studio 中修改可见性。
5. 点 **▶ 开始推流测试** → 软件推流至 YouTube 并进入**测试中**状态（观众看不到），此时请核对画面和声音。
6. 确认无误后点 **🔴 正式开播** → 状态变为**直播中**并开始计时，观众可通过分享链接观看。
7. 结束时点 **⏹ 结束直播**。

### 状态灯说明
| 状态 | 含义 |
|---|---|
| 未开播 / 已创建 | 尚未推流 |
| 连接中（闪烁） | 正在建立推流连接 |
| 测试中 | YouTube 已收到信号，仅自己可见 |
| 直播中（红色闪烁） | 观众可见，正在计时 |
| 已结束 | 本场直播完成 |

## 4. 分享到 Telegram

1. 创建直播后点 **📤 分享到 Telegram**。
2. 弹窗中会**先显示将要发送的消息内容**（标题 + 链接），可直接编辑。
3. 确认无误后点「确认发送」，消息即发送到设置中配置的群组。

分享链接格式：`https://youtube.com/live/{直播ID}?feature=share`

## 5. 定时直播

1. 在「直播」页下方「⏰ 定时直播」选择**当天**某个时间点（须晚于当前时间）。
2. 点「启动定时」后显示倒计时；到点软件自动完成「创建直播 → 推流 → 开播」全流程。
3. 倒计时期间可随时「取消定时」。

> 定时期间请保持软件运行（最小化到托盘亦可），并确保摄像头/声卡已连接。

## 6. 一边直播一边放 PPT

**支持外接屏放映**：PPT 在外接屏幕全屏放映、笔记本屏幕留作操作台。在「设置 → 采集设备 → 屏幕直播采集的屏幕」中选择你的外接屏（保存后一直生效），屏幕直播时观众看到的就是那块屏上的全屏 PPT。

典型讲道流程：

1. 摄像机源开播（观众看到讲台画面）。
2. 需要放投影时，在 PPT 里点「放映」→ 外接屏显示全屏 PPT。
3. 在笔记本上点软件的 **🖥 本机屏幕**（或迷你小窗上的 🖥）→ 观众看到外接屏的全屏 PPT（切换有 1~3 秒短暂停顿）。预览窗里显示的就是观众将看到的画面，切换前可先核对。
4. 放映结束后点 **📷** 切回摄像机画面。

> 屏幕直播时，Bethel Live 自己的窗口（含迷你小窗）已自动从采集画面中隐藏。麦克风声音全程不受切换影响。修改「采集的屏幕」设置后，重新切一次视频源即可生效。

> ⚠ **重要**：屏幕直播只采集**画面**——PPT 内嵌视频/音乐的**电脑声音不会进入直播**（观众只能听到麦克风）。需要播放带声音的内容时，请把电脑音频输出接入调音台、混进声卡的输入通道。

**画质选择**：「设置 → 直播默认值 → 直播画质」提供三档（高清 4.5M / 标准 2.5M / 流畅 1.2M）。场地网络上行带宽不足时选低档位，比高清但持续卡顿的观感好得多。直播中若推流中断，软件会自动重连（最多 5 次）；界面出现「🔄 重新推流」按钮时点击即可恢复——**观众链接始终不变**。

## 7. 外观主题

「设置 → 外观主题」提供六种配色：**深色**（默认）、**浅粉**、**浅红**、**浅紫**、**浅蓝**、**黑白**。点击色板立即生效并自动保存，重启后保持所选主题。

## 8. 窗口与托盘

- **迷你模式**：点标题栏旁的 ⧉ 按钮切换为置顶小窗（画中画），鼠标悬停可显示状态与「结束直播 ⏹ / 还原 ⤢」按钮。
- **最小化到托盘**：关闭窗口时可选择「最小化到托盘」，**正在进行的直播不会中断**；点托盘图标可随时唤回窗口。
- **关闭提示**：点关闭按钮会询问「退出软件 / 最小化到托盘」，勾选「记住我的选择」后不再询问（可在设置中改回「每次询问」）。
- **开机自启**：在「设置 → 系统」中开启。

## 9. 数据保存位置与软件更新

所有设置（API 凭据、Telegram 配置、设备与主题偏好）和 YouTube 授权令牌都保存在**系统用户数据目录**，与软件本体完全分离：

- macOS：`~/Library/Application Support/Bethel Live/`
- Windows：`%APPDATA%\Bethel Live\`

因此：

- **更新/覆盖安装软件不影响任何设置**，装完新版打开即用，无需重新填写；
- 敏感字段（Client Secret、Bot Token、授权令牌）在该目录中是**加密密文**（密钥由系统钥匙串/DPAPI 管理），拷走文件也无法解读；
- 每次保存设置时会自动保留上一版备份（`settings.json.bak`），误操作可找回；
- 只有手动删除该目录（或在设置里重新填写覆盖）才会清除这些数据。

> macOS 提示「Bethel Live 想访问钥匙串」时请点**「始终允许」**——拒绝会导致加密的密钥无法解读、显示为空。

## 10. 常见问题

**Q：点「连接 YouTube 账号」报错？**
检查设置中 Client ID / Client Secret 是否填写正确；浏览器授权页面须选择有直播权限的频道账号。

**Q：授权页提示「访问被阻止 / 应用未完成验证 / access_denied」？**
你的 Google Cloud 项目还在「测试」状态，只有加入测试用户名单的账号能授权——回到 Google Cloud Console 的「OAuth 同意屏幕 → 测试用户」，把教会账号邮箱加进去（见 2.1 第 4 步）。

**Q：推流测试提示 45 秒未收到信号？**
依次检查：网络是否可访问 YouTube；ffmpeg 是否安装（终端执行 `ffmpeg -version`）；摄像头是否被其他软件占用。

**Q：屏幕直播画面是黑的（macOS）？**
到「系统设置 → 隐私与安全性 → 屏幕录制」勾选 Bethel Live 后重启软件。

**Q：Telegram 发送失败 chat not found？**
确认机器人已加入目标群组，Chat ID 填写的是群组 ID（含 `-100` 前缀）。

**Q：直播有延迟？**
本软件使用低延迟编码（约数秒）。YouTube 端延迟可在 YouTube Studio 的直播设置中进一步调整。

## 11. 关于

「关于」页可查看软件版本、作者（Telegram @Dingjin2025）与免责声明。

**自动更新提醒**：软件启动后会自动检查新版本（此后每 4 小时一次），发现新版时界面顶部会出现提示横幅，点「前往下载」即可获取安装包，覆盖安装即完成升级（设置与授权都保留在本机，不会丢失）。也可随时在「关于 → 检查更新」手动检查。
